wisp-playground
===============

wisp dummy code

run server::

    npm install wisp
    npm install jsdom
    npm install swig
    npm install redis

    sudo rm -rf reddit_asianhotties.sock && sudo -u www-data wisp crawl_reddit_using_zombie.wisp
