wisp-playground
===============

wisp dummy code

run server::

    npm install wisp
    npm install jsdom
    npm install swig
    npm install redis

    rm -rf /tmp/reddit_asianhotties.sock && wisp crawl_reddit_using_zombie.wisp
